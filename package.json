{
  "name": "freecodecamp-headers",
  "version": "1.0.0",
  "description": "Parses the request header and outputs IP address, OS, and language.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0",
    "ua-parser": "^0.3.5",
    "ua-parser2": "^0.1.37"
  }
}
